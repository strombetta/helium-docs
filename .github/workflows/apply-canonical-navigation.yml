name: Canonical navigation migration

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - name: Report completed migration
        run: echo "Canonical navigation migration has already been applied."
