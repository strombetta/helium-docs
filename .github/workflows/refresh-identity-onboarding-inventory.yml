name: Refresh identity and onboarding inventory

on:
  pull_request:
    paths:
      - 'articles/identity/**'
      - 'articles/onboarding/**'
      - '.authoring/managed-files.txt'
      - '.github/workflows/refresh-identity-onboarding-inventory.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: identity-onboarding-inventory-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  refresh:
    if: github.event_name == 'workflow_dispatch' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest

    steps:
      - name: Checkout pull-request branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref || github.ref_name }}

      - name: Regenerate inventory
        run: python3 scripts/generate_content_inventory.py

      - name: Commit generated inventory
        shell: bash
        run: |
          git add planning/content-inventory.yml planning/url-migration-map.yml planning/content-coverage.md
          if git diff --cached --quiet; then
            echo "Inventory is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "4189826+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: refresh identity and onboarding inventory"
          git push origin HEAD:${{ github.head_ref || github.ref_name }}
